fastapi==0.115.6
uvicorn==0.34.0
httpx==0.28.1
beautifulsoup4==4.12.3
lxml==5.3.0
apscheduler==3.10.4
pydantic==2.10.4
python-dotenv==1.0.1
websockets==14.1
# Génération d'icônes PNG pour le manifest PWA (WebAPK Android exige du PNG)
Pillow==11.0.0

# Optionnel : source de prix MT5 (Windows uniquement, ou Linux/Mac via Wine)
# MetaTrader5==5.0.45

pytest==8.3.4
pytest-asyncio==0.25.0

# ML stack pour training offline (ml_train.py). scikit-learn a ~80 MB
# de deps transitifs. Non chargé au runtime prod (scoring live reste en
# Python pur), seulement utilisé pour training et chargement modèle joblib.
scikit-learn==1.5.2
pandas==2.2.3
joblib==1.4.2

# Auth multi-tenant : hashing password (Chantier 1 SaaS).
# bcrypt[standard] : bind natif, déjà packagé pour Linux/Win.
bcrypt==4.2.1
