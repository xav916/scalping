__pycache__/
*.pyc
.env
.venv/
venv/
*.egg-info/
dist/
build/
.mypy_cache/
.pytest_cache/

# Clés et secrets — ne JAMAIS committer
*.pem
*.key
scalping-key*

# Artefact de build Tailwind (généré par Docker, pas versionné)
frontend/css/tailwind.css
node_modules/

# Bases SQLite locales (runtime / tests) — pas de données de trade en repo
*.db
*.db-journal
*.db-wal
*.db-shm
kill_switch.json
mt5_sync_state.json

# Visual Companion mockups (brainstorming skill)
.superpowers/

# Stratégie personnelle (non partagée, jamais committée)
docs/strategy/

# Artefacts diagnostiques éphémères (scripts one-shot d'analyse, DBs locales)
_macro_veto_analysis/

# Outputs des scripts research (re-générables : pre_screen_universe.py +
# deep_dive_survivors.py). Pas versionnés pour éviter de polluer le repo
# avec des résultats jetables qui changent à chaque scan.
deep_dive_results.csv
pre_screen_results.csv

# Artefacts locaux d'outillage — jamais du code du projet
.claude/worktrees/
.omc/state/
bridge.log
